<!DOCTYPE html>
<html>
  <head>
    <title>TodoTutorial</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class="navbar bg-light mb-5">
      <div class="container-fluid">
        <a class="navbar-brand">JKT Todo</a>
        <% if !User.find_by(id: session[:user_id]).nil? %>
          <div class="d-flex">
            <span class="navbar-text pe-3">logged in as <%= User.find_by(id: session[:user_id]).name %></span>
            <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-secondary" %>
          </div>
        <% else %>
          <div class="d-flex">
            <%= button_to "Login", login_path, class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
